#include "monty.h"
/**
  *f_nop- a program that doesn't do anything
  *@head: head of stack
  *@counter: number
  *Return: no return value
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
